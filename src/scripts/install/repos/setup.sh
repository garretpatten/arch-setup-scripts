#!/bin/bash

# Arch does not use apt-style third-party repository manifests.
# AUR packages are handled by yay; pacman repos live in /etc/pacman.conf.
# This file exists to keep directory-tree parity with the Ubuntu sibling.

exit 0
